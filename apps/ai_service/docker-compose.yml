version: "3.8"

services:
  ai_service:
    build:
      context: .
      dockerfile: apps/ai_service/Dockerfile
    container_name: nexus_ai_service
    ports:
      - "8000:8000"
    env_file:
      - apps/ai_service/.env
    restart: unless-stopped
